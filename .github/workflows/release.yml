name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            suffix: linux-x86_64
            ext: ""
            src: linux-macos
          - os: windows-latest
            suffix: windows-x86_64
            ext: ".exe"
            src: windows
          - os: macos-latest
            suffix: macos-arm64
            ext: ""
            src: linux-macos
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: |
          gcc -O2 -o compressor-${{ matrix.suffix }}${{ matrix.ext }} ${{ matrix.src }}/compressor.c
          gcc -O2 -o decompressor-${{ matrix.suffix }}${{ matrix.ext }} ${{ matrix.src }}/decompressor.c

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          export PATH="/c/msys64/mingw64/bin:$PATH"
          gcc -O2 -o compressor-${{ matrix.suffix }}${{ matrix.ext }} ${{ matrix.src }}/compressor-win.c
          gcc -O2 -o decompressor-${{ matrix.suffix }}${{ matrix.ext }} ${{ matrix.src }}/decompressor-win.c

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.suffix }}
          path: |
            compressor-${{ matrix.suffix }}${{ matrix.ext }}
            decompressor-${{ matrix.suffix }}${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: '**/*'